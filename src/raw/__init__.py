"""
There's no code or pipeline in the raw layer for this project.

However, this layer is preserved for raw layer data pipeline that
pulls data from the raw source, via API or SQL database connection.

One example of this layer can be pulling data via API and store in S3.
"""
